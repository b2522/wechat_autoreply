## 环境要求

- **Windows** **微信 PC 端（4.x 版本）**

- Python 3

- 依赖已安装：`uiautomation`、`pillow`、`rapidocr_onnxruntime`

  ```
  pip install uiautomation pillow rapidocr_onnxruntime
  ```

## 如何使用

1. 修改start.bat中的python路径，绝对路径

2. 修改config.json中的群名、其他参数，详见readme.md文件

3. 把需要监控的微信群单独拉出来，置顶，可以放置在屏幕的右边，调窄一点

4. cmd窗口运行start.bat

5. 建议先测试，再使用

   **注意：不要锁屏，不要最小化需要监控的群******